## Status And Notes

Current stage:

1. objective defined
2. experiment reset to a clean stage-1 state

Immediate next goal:

- generate a sensible retrieval request draft from the experiment description
- identify the BAYC contract scope and the retrieval metrics needed to support MED generation

Known constraint:

- any previously present governance-like retrieval or proposal artifacts were intentionally removed because they were not coherent with BAYC

Entry points for the next step:

- root descriptor: `experiment.json`
- objective: `00-overview/00-objective.md`
